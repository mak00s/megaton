# Megaton credentials (service account JSON string; single line)
# Paste JSON without newlines. If you prefer multiline JSON, set CREDS_JSON in the notebook cell.
MEGATON_CREDS_JSON=

# Optional: auto-install override (1 to enable, 0 to disable)
MEGATON_AUTO_INSTALL=1

# Optional: install from a specific git ref (branch or commit)
MEGATON_GIT_REF=main

# Optional: smoke test targets
SMOKE_GS_URL=
SMOKE_BQ_PROJECT=
SMOKE_BQ_DATASET=
SMOKE_GSC_SITE_URL=
